# Decision Register — geraicuan

Updated: 2026-09-26

Record accepted decisions that materially constrain product behavior,
architecture, security, data, operations, or delivery. Repository evidence must
support each decision; AI output alone is not evidence.

| ID | Status | Decision | Drivers | Evidence | Supersedes |
|---|---|---|---|---|---|
| ADR-0001 | Accepted 2026-09-25 | Rebuild the CMS presentation layer from zero on official shadcn/ui blocks and the owner's HTML reference; domain layer unchanged ([ADR-0001](docs/adr/ADR-0001-ui-v3-rebuild.md)) | Owner judged the accreted UI imprecise; masking-first scope | Owner messages 2026-09-25; `~/Documents/work/notes/geraicuan-html/` | Spec 10 v2.x, spec 20 |
| D-14 | Accepted 2026-09-26 | GeraiCUAN is a free SaaS for gerai expedition aggregators with three cores: kirim, cetak resi (masked label), invoice (nota). Invoice = one immutable charge document per issued shipment, not payment evidence (spec 02 §v3.1, spec 05 DATA-14) | Owner direction 2026-09-26; boundary adopted from GeraiOS spec 11 |
| D-15 | Accepted 2026-09-26 | Invoice v3.1 charges the provider `price` + insurance only; no gerai extra-fee lines yet | Owner chose "Belum, ongkir saja" |
| D-16 | Accepted 2026-09-26 | The AdsBookCMS-shape `POST /order` probe is prepared but not run; T-153 stays the gate | Owner chose "Tunda dulu"; a live probe creates a real order |
| D-17 | Accepted 2026-09-26 | Mengantar-app look deltas (spec 10 §13) wait for the owner's analysis to finish; screens follow the owner's HTML reference meanwhile | Owner chose "Tunggu analisisnya selesai" |
| D-18 | Accepted 2026-09-26 | Adopt the Mengantar-app look (spec 10 §13 deltas D1–D6, D9, D11, D12; D7/D8 rejected for the 40+ floor) as spec 10 v3.2, applied once through global tokens and the shell after the Phase 18 screens land (T-228) | Owner: GeraiCUAN should look and behave like app.mengantar.com on shadcn/ui; chose "Setelah layar selesai". Supersedes D-17 |
| D-19 | Accepted 2026-09-26 | Pickup vehicle (Motor/Mobil/Truk) is stored and shown like handover type, not sent to Mengantar until T-153 | Mengantar's app shows a Volume choice on Pick Up (analysis §9.2); supersedes the earlier "no vehicle field" rule (spec 10 §12) |
| D-20 | Accepted 2026-09-26 | Add from the Mengantar study: label information editor with live preview, two-step print-format modal, Mengantar-style order detail, full parcel-status vocabulary | Owner selected all four |
| D-21 | Accepted 2026-09-26 | (a) Masuk (tenant and Super Admin) and Daftar split at ≥ 1024 px into a left visual panel (brand, headline, the three cores, static nota mock) and the card; below 1024 px the card alone. Daftar is three client-side steps (Akun, Gerai, Awalan) with one server submission; the Server Action stays the trust boundary. (b) A new or changed shipment prefix is 2–3 capitals/digits (e.g. PHI, A29); stored 4–5 character prefixes stay valid. Sign-up stores the chosen prefix unlocked (migration 0060: trigger guard, not `CHECK NOT VALID`, which would block allocation for legacy rows; `register_tenant_self_service_with_prefix` wraps the unchanged 0051 function) | Owner 2026-09-26: "login kiri design visual → kanan login … register bikin flow step … initial invoice … max 3 karakter misal a29, phi"; spec 02 PR-83/PR-44, spec 05 DATA-10/DATA-12, spec 17 UX-v3.10 |
| D-22 | Superseded by D-24 (2026-09-26) | GeraiCUAN brand colours replace Mengantar blue: navy `#0B2D4F` primary, green `#10B981` as accent (never white text on it), slate neutrals; contrast-adjusted values in spec 10 §2.0 (v3.3). Layout and anatomy from D-18 unchanged. Courier recap on Dasbor shows logos only | Owner supplied the palette ("gera cuan pakai pattern ini") and asked for logo-only couriers |
| D-23 | Accepted 2026-09-26 | UI terminology: roles read "Pemilik gerai" (`TENANT_ADMIN`), "Operator" (`OPERATOR`) and "Admin platform" (`SUPER_ADMIN`; login "Masuk Admin Platform", badge "Khusus tim GeraiCUAN"); the business is "gerai" in every rendered string. "Tenant" and "Super Admin" are internal terms only — code identifiers, enum and DB values, audit codes, routes (`/login/tenant`, `/login/super-admin`, `/platform/tenant`) and specs keep them. Spec 10 §7 | Owner: "kata tenant, kata super admin kurang cocok kalo ditampilin di public, buat kata yang lebih professional" |
| D-24 | Accepted 2026-09-26 | Keep the Mengantar-app colours (spec 10 v3.2 §2.1); the navy/green brand palette and the green top bar are withdrawn. Logo-only courier columns (Dasbor recap, Laporan totals) stay | Owner: "kembalikan pakai mengantar aja visual colornya … hijau brands tadi kurang" |
| D-25 | Accepted 2026-09-26 | Pickup slots are one-hour windows 08.00–17.00 WIB (starts 08:00–16:00), still ≥ 90 min ahead today. This is a GeraiCUAN-side schedule: Mengantar's documented `POST /time` window is 09:00–18:00 (unverified, spec 05 DATA-13) and no slot is sent until T-153. DB CHECK widened additively to `^(0[89]\|1[0-7]):00$` (0061); 17:00 stays valid only for pre-T-234 drafts. Spec 02 PR-70 | Owner 2026-09-26: "pilihan pickup start jam 8 pagi sampai jam 17.00" (T-234) |
| D-26 | Accepted 2026-09-26 | Rebuild the Mengantar order payload offline to the documented shape (`api-public.mengantar.com/docs`: `{courier, pickup:{type, address_id, time_id, volume}, orders:[…]}`), then run ONE owner-approved live non-COD drop-off order as the T-153 proof | Owner chose "Bangun offline + 1 uji live"; the live call still needs explicit approval at run time |
| D-27 | Accepted 2026-09-26 | Pickup slots follow Mengantar `POST /time`: 09.00–18.00, ≥ 90 min ahead (supersedes the 08.00–17.00 of D-25; drafts holding 08:00 stay valid and are re-picked when sent) | Owner chose "Ikut Mengantar" |
| D-28 | Accepted 2026-09-26 | COD Ongkir stays: the courier COD amount = ongkir + biaya COD (goods value 0, already paid at the gerai), computed automatically and shown to the operator — consistent with the documented COD definition | Owner: "nilai cod = nilai ongkir + biaya cod" |
| D-29 | Accepted 2026-09-26 | Ninja is removed (Mengantar discontinued it on 2026-09-01, `COURIER_DISABLED`): not offered, not in the courier catalogue or display map, logo deleted; historical Ninja shipments stay readable as the plain name "Ninja" (text fallback) | Owner chose "Hapus dari pilihan", then "ninja hapus aja" (2026-09-26) |
| D-30 | Accepted 2026-09-26 (coordinator defaults, owner informed) | Webhook receiver built and tested offline but kept closed until the account has it enabled; no Mengantar dropshipper feature (masking stays on our label); receiver score and courier-performance calls off (they send customer phones); cancel waits for T-153; settlement view stays off; COD is not offered when `unsupportedCodCheckFirstSap` is true | Gap analysis 2026-09-26 recommendations |
| D-31 | Accepted 2026-09-26 | "Info terbaru" is a platform-wide announcement feed: the Admin platform writes it at `/platform/info` (draft, publish, pin, unpublish; audited), every gerai member (both roles, a pending gerai included) reads published items at `/app/info` with per-user read state; the menu item sits above Dasbor, but login still lands on Dasbor (`/app`) — no remembered-page logic. Body is plain text (line breaks only), never HTML/markdown | Owner 2026-09-26: "tambahkan menu Info terbaru di atas Dasbor, namun default masuk tetap Dasbor"; Mengantar app analysis §9.0/§9.14 |
| D-32 | Accepted 2026-09-26 | A local, read-only Kemendagri wilayah reference (`wilayah_areas`: kecamatan + kelurahan/desa + upstream kode pos, DATA-22) **suggests** destination areas while typing; the Mengantar area `_id` from a live provider search on the outlet's account stays the **sole** destination authority. Picking a suggestion runs one guarded provider lookup (≤ 3 keywords, strict kecamatan + city match; ambiguous → the person chooses; nothing → today's free-text provider search). `destination_area_verified_at`, `validateMengantarDestinationAreaSelection` and the hidden-field contract are unchanged; no tenant table stores a wilayah code or kode pos yet. Data: upstream `cahyadsn/wilayah` (README: Kepmendagri 300.2.2-2430/2025) + `cahyadsn/wilayah_kodepos`, pinned by commit and vendored under `data/wilayah/` with MIT licences and checksums; kode pos is a hint only. Search uses `pg_trgm` (production availability is a release gate, RELEASE.md) | Owner 2026-09-26: "db kecamatan, kelurahan jika perlu, kode pos … biar gampang dan load cepat"; owner accepted every recommendation of the T-245 proposal the same day | T-245: migration 0067, `tests/wilayah-t245.integration.test.ts`, `tests/location-search-busy-t245.integration.test.ts`, BUILD-LOG T-245 entry (EXPLAIN ANALYZE on the full 91,047-row import) | TD-16 / DATA-7 clauses "no local location cache/table" and "no canonical kecamatan dataset is introduced" |
| D-33 | Accepted 2026-09-26 (coordinator decision on review M1) | Mengantar cancelling an unpaid order is accepted: `AWAITING_UPSTREAM_PAYMENT → CANCELLED` is an allowed provider transition in both the TypeScript graph and the webhook's SQL twin (additive migration 0068), and unpaid recovery refuses a shipment that is `CANCELLED` even though its order snapshot still reads unpaid (defence in depth). Reachability is a known gap: pull and webhook match only by AWB, which an unpaid order lacks (TASKS T-247 open item) | Review 2026-09-26 M1: the shipment stayed "Menunggu pembayaran" and pay-unpaid was still offered for a cancelled order | `tests/unpaid-recovery.integration.test.ts`, `tests/mengantar-tracking-t238.integration.test.ts`, `tests/provider-delivery-vocabulary.integration.test.ts`; spec 05 DATA-18 | — |
| D-34 | Accepted 2026-09-26 (review L3) | An issued invoice keeps the gerai logo it was issued with: every saved logo is kept content-addressed in `tenant_logo_versions` (tenant-scoped, append-only, same RLS shape as `tenant_brand_settings`); `shipment_invoices.logo_sha256` records the version at issuance and the nota renders that version through `/app/brand/logo?sha=` (tenant from the session). Invoices issued before 0068 carry no snapshot and render no logo rather than today's logo. Chosen over copying bytes onto every invoice (duplicates up to 200 KB per invoice) | Review 2026-09-26 L3: the page said "tetap seperti saat diterbitkan" while the logo followed the current setting | `tests/mengantar-tracking-t238.integration.test.ts` (T-247 L3), `tests/gerai-brand-t243.integration.test.ts`; spec 05 DATA-14, DATA-20 | — |

Use stable IDs such as `DEC-001`. When a decision needs detailed alternatives or
consequences, add a repository-owned ADR and link it from this register. Never
rewrite history silently: mark the old decision superseded and add the new one.
